FROM tiangolo/uwsgi-nginx-flask:python3.7 AS build
RUN apt-get update && apt-get install -y --no-install-recommends apt-utils && apt-get install -y unixodbc-dev

FROM build

ARG STAGE
ENV LISTEN_PORT 47200
EXPOSE 47200
ENV PYTHON_SOLUTION_PATH=''
ENV PYTHON_ENV=${STAGE}
ENV UWSGI_INI /app/Potter/FacebookAccounts/Api/uwsgi.ini


COPY ./Potter /app/Potter
COPY ./Core /app/Core
COPY ./requirements.txt /app/Potter/FacebookAccounts/Api

WORKDIR /app/Potter/FacebookAccounts/Api
RUN pip install --upgrade pip && pip3 install -r requirements.txt