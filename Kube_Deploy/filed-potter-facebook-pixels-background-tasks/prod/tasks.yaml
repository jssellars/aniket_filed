apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-filed-potter-facebook-pixels-background-tasks
spec:
  selector:
    matchLabels:
      run: prod-filed-potter-facebook-pixels-background-tasks
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1       
      maxUnavailable: 0
  template:
    metadata:
      labels:
        run: prod-filed-potter-facebook-pixels-background-tasks
    spec:
      containers:
      - name: prod-filed-potter-facebook-pixels-background-tasks
        image: ct.filed.com/prod-filed-potter-facebook-pixels-background-tasks:v0.1
        imagePullPolicy: Always
        # livenessProbe:
        #   httpGet:
        #     path: /
        #     port: 80
        #   initialDelaySeconds: 5
        #   periodSeconds: 5  
        ports: 
        - containerPort : 47251
      imagePullSecrets:
        - name: regcred  