apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev2-filed-ad-preview
spec:
  selector:
    matchLabels:
      run: dev2-filed-ad-preview
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1       
      maxUnavailable: 0
  template:
    metadata:
      labels:
        run: dev2-filed-ad-preview
    spec:
      hostAliases:
        - ip: "192.168.1.211"
          hostnames:
            - "dev2.filed.com"
      imagePullSecrets:
        - name: regcred
      containers:
        - name: dev2-filed-ad-preview
          image: ct.filed.com/dev2-filed-ad-preview:v0.1
          imagePullPolicy: Always
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: 80
        #   initialDelaySeconds: 5
        #   periodSeconds: 5  
        ports: 
        - containerPort : 47221
        volumeMounts:
        - name: persistent-storage
          mountPath: /app/logs
      volumes:
      - name: persistent-storage
        persistentVolumeClaim:
          claimName: dev2-mss-logs-claim
