FROM tiangolo/uwsgi-nginx-flask:python3.7 AS build
RUN apt-get update && apt-get install -y unixodbc-dev

FROM build

ENV LISTEN_PORT 42201
EXPOSE 42201
ENV PYTHON_SOLUTION_PATH=''
ENV PYTHON_ENV=dev
ENV UWSGI_INI /app/Potter/FacebookAccounts/BackgroundTasks/uwsgi.ini

COPY ./Potter /app/Potter
COPY ./Core /app/Core
COPY ./requirements.txt /app/Potter/FacebookAccounts/BackgroundTasks

WORKDIR /app/Potter/FacebookAccounts/BackgroundTasks
RUN pip install --upgrade pip && pip3 install -r requirements.txt