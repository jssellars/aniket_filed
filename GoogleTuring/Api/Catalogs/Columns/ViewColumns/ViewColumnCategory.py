from Core.Tools.Misc.Enumeration import Enumeration


class ViewColumnCategory:
    common = Enumeration(0, "common", "Common")
    # settings = Enumeration(1, "settings", "Settings")
    # performance = Enumeration(2, "performance", "Performance")
    # engagement = Enumeration(3, "engagement", "Engagement")
    # conversion = Enumeration(4, "conversion", "Conversion")
