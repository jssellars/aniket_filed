FROM tiangolo/uwsgi-nginx-flask:python3.7 AS build
RUN apt-get update && apt-get install -y --no-install-recommends apt-utils && apt-get install -y unixodbc-dev

FROM build

ARG STAGE
ENV LISTEN_PORT 47350
EXPOSE 47350
ENV PYTHON_SOLUTION_PATH=''
ENV PYTHON_ENV=${STAGE}
ENV UWSGI_INI /app/FacebookTuring/Api/uwsgi.ini

COPY ./FacebookTuring /app/FacebookTuring
COPY ./Core /app/Core
COPY ./requirements.txt /app/FacebookTuring/Api
COPY ./FacebookTuring/uwsgi.ini /app/FacebookTuring/Api

WORKDIR /app/FacebookTuring/Api
RUN pip install --upgrade pip && pip3 install -r requirements.txt